import {styled} from 'solid-styled-components';

export const Base = styled.div({
  display: 'grid',
  placeContent: 'center',
  width: '3em',
  aspectRatio: '1 / 1',
  borderRadius: '0.5em',
  border: '2px solid black',
  borderWidth: '1px',
});
