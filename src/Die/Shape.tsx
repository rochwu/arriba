import {styled} from 'solid-styled-components';

export const Shape = styled.div({
  display: 'grid',
  placeContent: 'center',
  width: '3em',
  aspectRatio: '1 / 1',
  borderRadius: '0.5em',
  borderWidth: '1px',
  borderStyle: 'solid',
  position: 'relative',
});
